version: "3.9"
services:
  api:
    build: ../
    ports: ["8000:8000"]
    env_file: ../.env
    volumes: ["../src:/app/src", "../config:/app/config"]
    # Future databases (uncomment in Phase 4):
    # depends_on: [postgres, qdrant, redis]

  # postgres:
  #   image: postgres:16
  #   environment: { POSTGRES_DB: qa_agent, POSTGRES_USER: qa, POSTGRES_PASSWORD: dev }
  #   ports: ["5432:5432"]
  # qdrant:
  #   image: qdrant/qdrant:latest
  #   ports: ["6333:6333"]
  # redis:
  #   image: redis:7-alpine
  #   ports: ["6379:6379"]
