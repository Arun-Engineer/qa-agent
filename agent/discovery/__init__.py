"""agent/discovery — Phase 2 building blocks for the autonomous QA agent.

Exposes:
    app_model   — typed shapes for the discovered application
    cred_vault  — run-scoped credential store (never persisted)
    crawler     — bounded Playwright discovery crawler
"""
